services:
  broker:
    build: .
    image: facet-broker:latest
    container_name: facet-broker
    restart: unless-stopped

    # Loopback only. Tailscale Funnel is what publishes this; binding 0.0.0.0
    # would additionally expose it to the LAN, which nothing needs.
    ports:
      - "127.0.0.1:8080:8080"

    env_file: broker.env

    volumes:
      - art-cache:/cache

    # It needs to fetch images and atomically persist per-user OAuth/countdown
    # state in one directory. Protect and back up this credential-bearing volume.
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    mem_limit: 256m
    cpus: "1.0"
    pids_limit: 128
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  art-cache:
