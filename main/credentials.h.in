/*
 * GENERATED FILE — do not edit, and do not commit the generated copy.
 *
 * CMake writes this to build/generated/credentials.h from the .env at the
 * project root, at configure time. Editing .env re-triggers the configure, so
 * a plain `idf.py build` always picks up new values.
 */
#pragma once

#define WIFI_SSID         "@WIFI_SSID@"
#define WIFI_PASSWORD     "@WIFI_PASSWORD@"

#define STT_ENDPOINT_URL  "@STT_ENDPOINT_URL@"
#define STT_BEARER_TOKEN  "@STT_BEARER_TOKEN@"

#define TIMEZONE          "@TIMEZONE@"
#define NTP_SERVER        "@NTP_SERVER@"

#define UNSPLASH_KEY      "@UNSPLASH_KEY@"
#define UNSPLASH_QUERY    "@UNSPLASH_QUERY@"
